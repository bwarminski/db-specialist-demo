# ABOUTME: Provides the base controller for the demo JSON endpoints.
# ABOUTME: Uses the API controller stack because the demo only serves JSON responses.
class ApplicationController < ActionController::API
end
