# Journal

- 2026-04-24: Task 3 on `wip/mixed-todo-api` adds only the Todo JSON API surface and tests. Preserve the intentionally broken schema assumptions from `db/schema.rb`; do not add a status index or widen scope into count/search routes.
- 2026-04-24: Task 5 seed retune uses a standalone `test/seeds_test.rb` Minitest shelling into `rails runner -e benchmark` because `rails test` still forces the SQLite `test` environment and dies on the Postgres-only seed SQL.
- 2026-04-24: Running `SECRET_KEY_BASE=test RAILS_ENV=benchmark bundle exec ruby -Itest test/controllers/todos_controller_test.rb` hung with Postgres showing an `idle in transaction` backend, so keep that verification path separate from the focused seed check until the benchmark test harness is cleaned up.
- 2026-04-25: Task 10 adds `.github/workflows/verify-fixture.yml` with a Postgres service, app + collector bundle setup, `createdb checkpoint_demo`, and the exact `checkpoint-collector/bin/load verify-fixture` command. Local verification currently fails on the search explain drift check, so CI wiring is in place but the verifier target is not green yet.
- 2026-04-25: Task 2 keeps controller request tests self-contained in `test/test_helper.rb`; auto-loading `db/seeds.rb` there breaks the requested `rails test` loop before assertions run because the command still boots SQLite/test while seeds use Postgres-only SQL.
- 2026-04-25: Follow-up coverage for Task 2 locks the `USER_COUNT=7`, `ROWS_PER_TABLE=21` round-robin seed distribution as three todos per user id `1..7`, and keeps the unsupported-order controller test on the invalid-order path by providing a valid `user_id`.
