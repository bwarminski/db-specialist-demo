# ABOUTME: Loads the demo Rails rake tasks for database and test commands.
# ABOUTME: Lets `bundle exec rails db:setup` run against the handcrafted app.
require_relative "config/application"

Rails.application.load_tasks
